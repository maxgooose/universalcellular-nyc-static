# Netlify: publish the static mirror. Pages use real directory structure
# (collections/apple/index.html), so directory-index resolution covers
# pretty URLs — no per-route redirects needed.

[build]
  publish = "techrecomm-mirror/site"
